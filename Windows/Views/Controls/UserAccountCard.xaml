<UserControl x:Class="ZMusic.Views.Controls.UserAccountCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Border Cursor="Hand"
            CornerRadius="8"
            Padding="14,12">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{ui:ThemeResource SubtleFillColorSecondaryBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Width="36" Height="36" VerticalAlignment="Center">
                <Ellipse x:Name="FallbackAvatar"
                         Fill="{ui:ThemeResource ControlFillColorDefaultBrush}" />
                <TextBlock x:Name="InitialText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" />
                <Ellipse x:Name="AvatarEllipse" Visibility="Collapsed">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="AvatarBrush" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>

            <StackPanel Grid.Column="1"
                        Margin="12,0,2,0"
                        VerticalAlignment="Center">
                <TextBlock x:Name="NameText"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Margin="0,2,0,0"
                           FontSize="11"
                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                           Text="已登录" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
