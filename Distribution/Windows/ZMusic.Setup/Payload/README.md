# Placeholder

`build.py` copies the **Silent** MSI here as `ZMusic.msi` before compiling Setup so it can be embedded.
Do not commit real MSI binaries. The distributable UI MSI is written only to `artifacts/windows/ZMusic-UI.msi`.
