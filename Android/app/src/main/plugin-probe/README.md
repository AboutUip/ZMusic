# 内置调试探针

源码随应用编译打成 `.zpp`（Gradle `packPluginProbe`）。**插件引擎调试**开启时必定加载；关闭则不运行。

本包定义调优页 `tune`。调试开启时宿主把它当作 Dock / 侧栏的一页来切换（不是叠层进入）。页内含控件与样式试验。不是给社区插件用的 Dock 槽位。投放目录里的同 id 包不会覆盖本包。

不要把生成的 `.zpp` 提交进仓库。
